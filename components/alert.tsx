const Alert = () => {
  return (
    <div className="flex h-12 items-center justify-center bg-primary">
      <p>
        Due to our new platform, existing Sampit Sport customers will have to set up new account.
      </p>
    </div>
  );
};

export default Alert;
